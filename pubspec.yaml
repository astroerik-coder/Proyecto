name: yugioh
description: "Project of Yugioh"
publish_to: 'none' 

version: 1.0.0+1

environment:
  sdk: '>=3.4.3 <4.0.0'
dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.6

dev_dependencies:
  flutter_test:
    sdk: flutter
  
  flutter_lints: ^3.0.0
  provider: ^6.1.2   # Manejo de estado con provider
  http: ^1.2.2      # Para realizar peticiones HTTP


flutter:

  uses-material-design: true
